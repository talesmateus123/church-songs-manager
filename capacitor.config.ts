import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'church-songs-manager',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
